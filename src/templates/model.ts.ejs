import {v4 as uuid} from 'uuid';

export class <%= props.firstLetterCapital %>> {
	public id: string;

	// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
	constructor(id?: string, ...args: any) {
		Object.assign(this, ...args);

		if (id) {
			this.id = id;
		} else if (id == '') {
			this.id = uuid();
		}
	}
}
